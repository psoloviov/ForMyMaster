using System.Collections.Generic;

namespace Task_4
{
    public class GameLogic
    {
        private List<Deck> _table = new List<Deck>();

        public static void GameStart(ref Queue<Deck> player1Deck, ref Queue<Deck> player2Deck)
        {
            if (player1Deck != null || player2Deck != null)
            {
                
            }
                
        }
    }
}